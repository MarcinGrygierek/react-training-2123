const result = 1000;
const a = 100;
const b = 10;

// stary sposób
console.log('Wynik mnożenia liczb ' + a + ' i ' + b + ' wynosi ' + result);

// sposób ES6
console.log(`Wynik mnożenia liczb ${a} i ${b} wynosi ${result}`);
